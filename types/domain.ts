export enum ScheduleEndType {
  never = "never",
  until = "until",
  count = "count",
}

export enum HistoryStatus {
  pending = "pending",
  taken = "taken",
  missed = "missed",
}

export enum HistorySource {
  manual = "manual",
  quickCheck = "quickCheck",
  system = "system",
}
